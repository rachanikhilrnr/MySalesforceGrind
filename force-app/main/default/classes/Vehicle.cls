public class Vehicle {
	static integer maxSpeed = 180;
    static integer normalspeed = 60;
    //static integer fuelType = 'Diesel';
    public static void  startEngine(){
        System.debug('Engine is starting');
    }
    public static void stopEngine(){
        System.debug('Engine is stopped');
    }
}