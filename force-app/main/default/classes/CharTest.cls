public class CharTest {
    public static void method(){
        String str = '';
        System.debug(str);
        for(integer i=0;i<str.length();i++){
            integer val = str.charAt(i);
            System.debug(val);
        }
    }
}